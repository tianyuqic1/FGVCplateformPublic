# 工作树合并验收（2026-09-10）

目标分支：`codex/model-publication`。来源：`codex/dataset-import-queue`（`f87904f`，原 dcbe 工作树）。合并前目标分支的模型发布与训练参数改动已保存为 `3227ea4`；原工作树与分支保留。

## 冲突处理

- 保留当前数据集名称、自动 v1、UUID 身份、request_id 幂等与数据集扩充链路；异步导入队列适配该契约，不恢复旧版手填版本号。
- 保留模型发布、硬件监测和训练参数功能，以及 100000 张 / 5 GiB / 单图 32 MiB 上限、30 分钟上传时限。
- 持久化队列使用稳定版本 ID、请求指纹与租约 fencing。注册已提交但任务未确认时，恢复读取既有结果，不重复注册。
- 将来源的同号迁移重编号为 `20260910_0017`，接续 `0016`；重新生成 Go OpenAPI 代码。
- 更新浏览器与客户端测试以匹配当前数据集分组页面、名称输入和异步响应。

## 验证结果

- `go test ./...`、`go vet ./...` 通过。
- `go test -race ./internal/dataset ./internal/httpapi` 通过。
- 隔离测试数据库迁移至 `0017` 后，PostgreSQL 适配器全套 race 测试通过，含队列容量、并发领取、过期租约恢复、旧消费者结果隔离、已注册结果重放。
- Python 数据集 API 与计算测试：14 passed；存在两条第三方弃用警告。
- 前端全部非浏览器 smoke 脚本与生产构建通过，无重复 JSX 属性警告。
- Playwright 模拟 API 浏览器检查通过：上传期间交互、刷新恢复、排队/运行/完成/失败状态、完成后刷新数据集列表及正确详情链接。

## 范围边界

本次为本地工作树整合，不推送远程、不删除工作树。仅迁移隔离测试数据库，未迁移正在使用的开发数据库，也未重建或重启服务。浏览器检查使用模拟 API，不代表已在现有运行服务完成真实异步导入验收。启用队列需按 `docs/dataset-import.md` 迁移并更新 Go/compute 服务；本轮不重复执行 GPU 训练。
